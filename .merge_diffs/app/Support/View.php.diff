diff --git a/app/Support/View.php b/app/Support/View.php
new file mode 100644
index 0000000..84431b9
--- /dev/null
+++ b/app/Support/View.php
@@ -0,0 +1,22 @@
+<?php
+declare(strict_types=1);
+
+namespace App\Support;
+
+final class View
+{
+    /** @param array<string, mixed> $data */
+    public static function render(string $viewPath, array $data, string $basePath): string
+    {
+        $file = rtrim($basePath, '/') . '/app/Views/' . ltrim($viewPath, '/') . '.php';
+        if (!is_file($file)) {
+            return 'View not found.';
+        }
+
+        extract($data, EXTR_SKIP);
+        ob_start();
+        require $file;
+        $content = ob_get_clean();
+        return $content === false ? '' : $content;
+    }
+}
